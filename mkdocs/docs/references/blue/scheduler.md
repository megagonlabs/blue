:::blue.scheduler.Scheduler
---
!!! example
    The `SessionCleanupScheduler` class demonstrates how you can customize the core scheduling logic by extending the existing `Scheduler` class.
:::blue.platform.SessionCleanupScheduler
